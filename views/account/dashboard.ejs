<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="luxury-theme">
    
    <main class="luxury-dashboard">
        <!-- Premium Header Section -->
        <section class="premium-header">
            <div class="header-content">
                <div class="welcome-section">
                    <!-- TASK 3: H2 greeting based on account type -->
                    <h2 class="account-greeting">
                        <% if (user.account_type === 'Employee' || user.account_type === 'Admin') { %>
                            Welcome <%= user.firstName %>, <span class="role-badge"><%= user.account_type %></span>
                        <% } else { %>
                            Welcome <%= user.firstName %>
                        <% } %>
                    </h2>
                    
                    <h1 class="luxury-title">
                        <span id="dynamic-greeting">Good Morning</span>, 
                        <span class="accent-name"><%= user.firstName %></span>
                    </h1>
                    <p class="luxury-subtitle">Your premium automotive experience awaits</p>
                </div>
                <div class="luxury-time-display">
                    <div class="time-primary" id="luxury-time">Loading...</div>
                    <div class="date-secondary" id="luxury-date">Loading...</div>
                    <div class="weather-badge" id="luxury-weather">
                        <i class="fas fa-sync fa-spin"></i>
                        <span>Loading weather...</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- Account Management Section (TASK 3) -->
        <section class="account-management-section">
            <div class="management-card">
                <div class="management-header">
                    <h3><i class="fas fa-user-cog"></i> Account Management</h3>
                </div>
                
                <div class="management-content">
                    <!-- TASK 3: User Info Display -->
                    <div class="user-info-display">
                        <div class="info-row">
                            <span class="info-label">Full Name:</span>
                            <span class="info-value"><%= user.firstName %> <%= user.lastName %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Email:</span>
                            <span class="info-value"><%= user.email %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Account Type:</span>
                            <span class="info-value account-type <%= user.account_type.toLowerCase() %>">
                                <%= user.account_type %>
                            </span>
                        </div>
                    </div>
                    
                    <!-- TASK 3: Update Account Link -->
                    <div class="update-account-link">
                        <a href="/account/update/<%= user.userId || user.account_id %>" class="update-btn">
                            <i class="fas fa-user-edit"></i>
                            Update Account Information
                        </a>
                    </div>
                    
                    <!-- TASK 3: Inventory Management (Only for Employee/Admin) -->
                    <% if (user.account_type === 'Employee' || user.account_type === 'Admin') { %>
                        <div class="inventory-management-section">
                            <h3><i class="fas fa-cog"></i> Inventory Management</h3>
                            <p class="management-description">
                                As an <%= user.account_type %>, you have access to inventory management features.
                            </p>
                            <a href="/inv/manage" class="inventory-btn">
                                <i class="fas fa-car"></i>
                                Manage Inventory
                            </a>
                        </div>
                    <% } %>
                </div>
            </div>
        </section>

        <!-- Real-time Stats Grid -->
        <section class="stats-grid-premium">
            <div class="stat-card-luxury">
                <div class="stat-icon-luxury">
                    <i class="fas fa-eye"></i>
                </div>
                <div class="stat-content-luxury">
                    <div class="stat-value" id="viewed-count">0</div>
                    <div class="stat-label">Vehicles Viewed</div>
                </div>
                <div class="stat-trend positive">
                    <i class="fas fa-arrow-up"></i>
                    <span id="viewed-trend">0%</span>
                </div>
            </div>

            <div class="stat-card-luxury">
                <div class="stat-icon-luxury">
                    <i class="fas fa-heart"></i>
                </div>
                <div class="stat-content-luxury">
                    <div class="stat-value" id="saved-count">0</div>
                    <div class="stat-label">Saved Vehicles</div>
                </div>
                <div class="stat-trend positive">
                    <i class="fas fa-plus"></i>
                    <span id="saved-trend">0 new</span>
                </div>
            </div>

            <div class="stat-card-luxury">
                <div class="stat-icon-luxury">
                    <i class="fas fa-search"></i>
                </div>
                <div class="stat-content-luxury">
                    <div class="stat-value" id="search-count">0</div>
                    <div class="stat-label">Recent Searches</div>
                </div>
                <div class="stat-trend neutral">
                    <i class="fas fa-clock"></i>
                    <span id="search-time">Just now</span>
                </div>
            </div>

            <div class="stat-card-luxury">
                <div class="stat-icon-luxury">
                    <i class="fas fa-user-clock"></i>
                </div>
                <div class="stat-content-luxury">
                    <div class="stat-value" id="session-timer">00:00</div>
                    <div class="stat-label">Session Time</div>
                </div>
                <div class="stat-trend neutral">
                    <i class="fas fa-play"></i>
                    Active
                </div>
            </div>
        </section>

        <!-- Main Content Grid -->
        <section class="premium-content-grid">
            <!-- Quick Vehicle Search -->
            <div class="premium-card">
                <div class="card-header">
                    <h2><i class="fas fa-search"></i> Quick Vehicle Search</h2>
                    <div class="live-badge">
                        <i class="fas fa-circle"></i>
                        Live
                    </div>
                </div>
                <div class="search-widget-premium">
                    <div class="search-input-group">
                        <i class="fas fa-search"></i>
                        <input type="text" id="vehicle-search" placeholder="Search makes, models, features..." class="luxury-input">
                    </div>
                    <div class="search-filters">
                        <select class="luxury-select" id="price-filter">
                            <option value="">Any Price</option>
                            <option value="0-30000">Under $30K</option>
                            <option value="30000-60000">$30K - $60K</option>
                            <option value="60000-100000">$60K - $100K</option>
                            <option value="100000+">$100K+</option>
                        </select>
                        <select class="luxury-select" id="type-filter">
                            <option value="">All Types</option>
                            <option value="suv">SUV</option>
                            <option value="sedan">Sedan</option>
                            <option value="truck">Truck</option>
                            <option value="luxury">Luxury</option>
                        </select>
                    </div>
                    <div class="search-results-live" id="search-results">
                        <div class="no-results">Start typing to search our premium inventory</div>
                    </div>
                </div>
            </div>

            <!-- Recently Viewed -->
            <div class="premium-card">
                <div class="card-header">
                    <h2><i class="fas fa-history"></i> Recently Viewed</h2>
                    <div class="update-time" id="recent-update">Updated just now</div>
                </div>
                <div class="recent-vehicles" id="recent-vehicles">
                    <div class="no-recent">
                        <i class="fas fa-eye-slash"></i>
                        <p>No recently viewed vehicles</p>
                        <small>Vehicles you view will appear here</small>
                    </div>
                </div>
            </div>

            <!-- Live Inventory Updates -->
            <div class="premium-card">
                <div class="card-header">
                    <h2><i class="fas fa-bolt"></i> Live Inventory</h2>
                    <div class="live-badge pulse">
                        <i class="fas fa-circle"></i>
                        Real-time
                    </div>
                </div>
                <div class="inventory-updates-live">
                    <div class="update-stream" id="inventory-stream">
                        <div class="update-item new-arrival">
                            <div class="update-icon">
                                <i class="fas fa-plus"></i>
                            </div>
                            <div class="update-content">
                                <p>New 2024 BMW 5 Series added</p>
                                <span class="update-time" id="update-time-1">Just now</span>
                            </div>
                        </div>
                        <div class="update-item price-drop">
                            <div class="update-icon">
                                <i class="fas fa-arrow-down"></i>
                            </div>
                            <div class="update-content">
                                <p>Price drop on Mercedes-Benz E-Class</p>
                                <span class="update-time" id="update-time-2">2 min ago</span>
                            </div>
                        </div>
                    </div>
                    <div class="inventory-stats">
                        <div class="inventory-stat">
                            <span class="stat-number" id="total-vehicles">0</span>
                            <span class="stat-label">Total Vehicles</span>
                        </div>
                        <div class="inventory-stat">
                            <span class="stat-number" id="new-today">0</span>
                            <span class="stat-label">New Today</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="premium-card">
                <div class="card-header">
                    <h2><i class="fas fa-rocket"></i> Quick Actions</h2>
                </div>
                <div class="quick-actions-premium">
                    <button class="action-btn-premium" onclick="scheduleTestDrive()">
                        <div class="action-icon">
                            <i class="fas fa-calendar-check"></i>
                        </div>
                        <span>Schedule Test Drive</span>
                        <div class="action-badge">Fast</div>
                    </button>
                    <button class="action-btn-premium" onclick="contactConcierge()">
                        <div class="action-icon">
                            <i class="fas fa-headset"></i>
                        </div>
                        <span>Contact Concierge</span>
                        <div class="action-badge">24/7</div>
                    </button>
                    <button class="action-btn-premium" onclick="viewSavedVehicles()">
                        <div class="action-icon">
                            <i class="fas fa-heart"></i>
                        </div>
                        <span>Saved Vehicles</span>
                        <div class="action-badge" id="saved-badge">0</div>
                    </button>
                    <button class="action-btn-premium" onclick="downloadBrochure()">
                        <div class="action-icon">
                            <i class="fas fa-download"></i>
                        </div>
                        <span>Premium Catalog</span>
                        <div class="action-badge">PDF</div>
                    </button>
                </div>
            </div>
        </section>

        <!-- Real-time Market Insights -->
        <section class="premium-card full-width">
            <div class="card-header">
                <h2><i class="fas fa-chart-line"></i> Real-time Market Insights</h2>
                <div class="live-badge">
                    <i class="fas fa-sync fa-spin"></i>
                    Live Data
                </div>
            </div>
            <div class="market-insights">
                <div class="insight-item">
                    <div class="insight-value" id="price-trend">$45,230</div>
                    <div class="insight-label">Average Vehicle Price</div>
                    <div class="insight-change positive">
                        <i class="fas fa-arrow-up"></i>
                        +2.3%
                    </div>
                </div>
                <div class="insight-item">
                    <div class="insight-value" id="inventory-count">127</div>
                    <div class="insight-label">Available Vehicles</div>
                    <div class="insight-change positive">
                        <i class="fas fa-plus"></i>
                        +5 today
                    </div>
                </div>
                <div class="insight-item">
                    <div class="insight-value" id="view-count">1,284</div>
                    <div class="insight-label">Daily Views</div>
                    <div class="insight-change positive">
                        <i class="fas fa-eye"></i>
                        +12.7%
                    </div>
                </div>
            </div>
        </section>
    </main>

    <script>
        // Real-time Clock and Date
        function updateLuxuryTime() {
            const now = new Date();
            
            // Update time
            const timeOptions = { 
                hour: '2-digit', 
                minute: '2-digit',
                second: '2-digit',
                hour12: false
            };
            document.getElementById('luxury-time').textContent = now.toLocaleTimeString('en-US', timeOptions);
            
            // Update date
            const dateOptions = { 
                weekday: 'long', 
                year: 'numeric', 
                month: 'long', 
                day: 'numeric' 
            };
            document.getElementById('luxury-date').textContent = now.toLocaleDateString('en-US', dateOptions);
            
            // Update greeting
            updateDynamicGreeting(now);
            
            // Update relative times
            updateRelativeTimes(now);
        }

        function updateDynamicGreeting(now) {
            const hour = now.getHours();
            let greeting;
            
            if (hour < 12) {
                greeting = "Good Morning";
            } else if (hour < 17) {
                greeting = "Good Afternoon";
            } else {
                greeting = "Good Evening";
            }
            
            document.getElementById('dynamic-greeting').textContent = greeting;
        }

        // Session Timer
        let sessionStart = Date.now();
        function updateSessionTimer() {
            const now = Date.now();
            const diff = now - sessionStart;
            const minutes = Math.floor(diff / (1000 * 60));
            const seconds = Math.floor((diff % (1000 * 60)) / 1000);
            
            const formattedTime = 
                String(minutes).padStart(2, '0') + ':' +
                String(seconds).padStart(2, '0');
            
            document.getElementById('session-timer').textContent = formattedTime;
        }

        // Simulated Real-time Data Updates
        function updateRealTimeStats() {
            // Simulate vehicle views
            const viewedCount = Math.floor(Math.random() * 5) + 10;
            document.getElementById('viewed-count').textContent = viewedCount;
            document.getElementById('viewed-trend').textContent = `${Math.floor(Math.random() * 10) + 5}%`;
            
            // Simulate saved vehicles
            const savedCount = Math.floor(Math.random() * 3) + 3;
            document.getElementById('saved-count').textContent = savedCount;
            document.getElementById('saved-badge').textContent = savedCount;
            document.getElementById('saved-trend').textContent = `${Math.floor(Math.random() * 2) + 1} new`;
            
            // Simulate search activity
            const searchCount = Math.floor(Math.random() * 4) + 6;
            document.getElementById('search-count').textContent = searchCount;
            
            // Update inventory counts
            document.getElementById('total-vehicles').textContent = Math.floor(Math.random() * 50) + 100;
            document.getElementById('new-today').textContent = Math.floor(Math.random() * 8) + 2;
            
            // Update market insights
            document.getElementById('price-trend').textContent = `$${(Math.random() * 10000 + 40000).toLocaleString()}`;
            document.getElementById('inventory-count').textContent = Math.floor(Math.random() * 50) + 100;
            document.getElementById('view-count').textContent = Math.floor(Math.random() * 500) + 1000;
        }

        // Update relative timestamps
        function updateRelativeTimes(now) {
            const recentUpdate = document.getElementById('recent-update');
            const sessionSeconds = Math.floor((now - sessionStart) / 1000);
            
            if (sessionSeconds < 60) {
                recentUpdate.textContent = 'Updated just now';
            } else if (sessionSeconds < 3600) {
                const minutes = Math.floor(sessionSeconds / 60);
                recentUpdate.textContent = `Updated ${minutes} min ago`;
            } else {
                recentUpdate.textContent = 'Updated today';
            }
            
            // Update inventory stream timestamps
            const update1 = document.getElementById('update-time-1');
            const update2 = document.getElementById('update-time-2');
            
            update1.textContent = sessionSeconds < 120 ? 'Just now' : `${Math.floor(sessionSeconds / 60)} min ago`;
            update2.textContent = sessionSeconds < 300 ? '2 min ago' : `${Math.floor((sessionSeconds - 120) / 60)} min ago`;
        }

        // Simulate weather data
        function updateWeather() {
            const weatherConditions = ['Sunny', 'Partly Cloudy', 'Cloudy', 'Clear'];
            const temperatures = [68, 72, 75, 70, 73];
            const icons = ['fa-sun', 'fa-cloud-sun', 'fa-cloud', 'fa-moon'];
            
            const randomCondition = weatherConditions[Math.floor(Math.random() * weatherConditions.length)];
            const randomTemp = temperatures[Math.floor(Math.random() * temperatures.length)];
            const randomIcon = icons[Math.floor(Math.random() * icons.length)];
            
            const weatherElement = document.getElementById('luxury-weather');
            weatherElement.innerHTML = `<i class="fas ${randomIcon}"></i><span>${randomTemp}° • ${randomCondition}</span>`;
        }

        // Initialize all real-time features
        document.addEventListener('DOMContentLoaded', function() {
            // Start real-time updates
            updateLuxuryTime();
            updateSessionTimer();
            updateRealTimeStats();
            updateWeather();
            
            // Set up intervals
            setInterval(updateLuxuryTime, 1000);
            setInterval(updateSessionTimer, 1000);
            setInterval(updateRealTimeStats, 5000); // Update stats every 5 seconds
            setInterval(updateWeather, 30000); // Update weather every 30 seconds
            
            // Simulate initial data
            setTimeout(() => {
                document.getElementById('search-time').textContent = '2 min ago';
            }, 2000);
        });

        // Quick action functions (placeholder)
        function scheduleTestDrive() {
            alert('Redirecting to test drive scheduling...');
        }

        function contactConcierge() {
            alert('Connecting you with our concierge service...');
        }

        function viewSavedVehicles() {
            alert('Showing your saved vehicles...');
        }

        function downloadBrochure() {
            alert('Downloading premium catalog...');
        }
    </script>

    <style>
        /* Checkerboard background only for desktop */
        @media (min-width: 961px) {
            .luxury-theme {
                background: url("/images/site/checkerboard.jpg") repeat center;
                background-size: 700px 700px;
            }
            
            /* Overlay for better readability */
            .luxury-theme::before {
                content: '';
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: linear-gradient(135deg, rgba(15, 23, 42, 0.85) 0%, rgba(30, 41, 59, 0.90) 100%);
                z-index: -1;
            }
            
            .luxury-dashboard {
                position: relative;
                z-index: 1;
            }
        }

        /* Mobile background */
        @media (max-width: 960px) {
            .luxury-theme {
                background: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
            }
        }

        /* Premium Luxury Theme */
        .luxury-theme {
            color: #e2e8f0;
            min-height: 100vh;
        }

        .luxury-dashboard {
            max-width: 1400px;
            margin: 0 auto;
            padding: 30px;
        }

        /* TASK 3: Account Greeting Styles */
        .account-greeting {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: #e2e8f0;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .role-badge {
            background: linear-gradient(135deg, #059669 0%, #10b981 100%);
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
        }

        /* TASK 3: Account Management Section */
        .account-management-section {
            margin-bottom: 30px;
        }

        .management-card {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.95) 0%, rgba(51, 65, 85, 0.95) 100%);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 30px;
            border: 1px solid rgba(71, 85, 105, 0.5);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .management-header {
            border-bottom: 1px solid rgba(71, 85, 105, 0.5);
            padding-bottom: 15px;
            margin-bottom: 20px;
        }

        .management-header h3 {
            font-size: 1.3rem;
            font-weight: 600;
            color: #60a5fa;
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 0;
        }

        .management-header h3 i {
            color: #60a5fa;
        }

        .user-info-display {
            background: rgba(15, 23, 42, 0.5);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 25px;
            border: 1px solid rgba(71, 85, 105, 0.3);
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid rgba(71, 85, 105, 0.2);
        }

        .info-row:last-child {
            border-bottom: none;
        }

        .info-label {
            color: #94a3b8;
            font-weight: 500;
            font-size: 0.95rem;
        }

        .info-value {
            color: #e2e8f0;
            font-weight: 600;
            font-size: 1rem;
        }

        .account-type {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            text-transform: capitalize;
        }

        .account-type.client {
            background: rgba(59, 130, 246, 0.1);
            color: #60a5fa;
            border: 1px solid rgba(59, 130, 246, 0.3);
        }

        .account-type.employee {
            background: rgba(34, 197, 94, 0.1);
            color: #22c55e;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }

        .account-type.admin {
            background: rgba(168, 85, 247, 0.1);
            color: #a855f7;
            border: 1px solid rgba(168, 85, 247, 0.3);
        }

        .update-account-link {
            margin-bottom: 30px;
            text-align: center;
        }

        .update-btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
            color: white;
            padding: 12px 24px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .update-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(59, 130, 246, 0.3);
            background: linear-gradient(135deg, #1e3a8a 0%, #2563eb 100%);
        }

        .update-btn i {
            font-size: 1.1rem;
        }

        /* TASK 3: Inventory Management Section */
        .inventory-management-section {
            background: rgba(15, 23, 42, 0.7);
            border-radius: 12px;
            padding: 25px;
            border: 1px solid rgba(34, 197, 94, 0.3);
            margin-top: 20px;
        }

        .inventory-management-section h3 {
            font-size: 1.2rem;
            color: #22c55e;
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 0 0 10px 0;
        }

        .management-description {
            color: #94a3b8;
            margin-bottom: 20px;
            font-size: 0.95rem;
        }

        .inventory-btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: linear-gradient(135deg, #059669 0%, #10b981 100%);
            color: white;
            padding: 12px 24px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .inventory-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(34, 197, 94, 0.3);
            background: linear-gradient(135deg, #047857 0%, #0d9488 100%);
        }

        .inventory-btn i {
            font-size: 1.1rem;
        }

        /* Premium Header */
        .premium-header {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.95) 0%, rgba(51, 65, 85, 0.95) 100%);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            margin-bottom: 30px;
            border: 1px solid rgba(71, 85, 105, 0.5);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            position: relative;
            overflow: hidden;
        }

        .premium-header::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, rgba(59, 130, 246, 0.1) 0%, transparent 70%);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .luxury-title {
            font-size: 2.5rem;
            font-weight: 300;
            margin: 0;
            background: linear-gradient(135deg, #e2e8f0 0%, #94a3b8 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .accent-name {
            background: linear-gradient(135deg, #60a5fa 0%, #3b82f6 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 600;
        }

        .luxury-subtitle {
            color: #94a3b8;
            font-size: 1.1rem;
            margin: 10px 0 0 0;
            font-weight: 300;
        }

        .luxury-time-display {
            text-align: right;
        }

        .time-primary {
            font-size: 2rem;
            font-weight: 200;
            font-family: 'Courier New', monospace;
            color: #60a5fa;
            margin-bottom: 5px;
        }

        .date-secondary {
            color: #cbd5e1;
            font-size: 1rem;
            margin-bottom: 10px;
        }

        .weather-badge {
            background: rgba(96, 165, 250, 0.1);
            border: 1px solid rgba(96, 165, 250, 0.3);
            border-radius: 20px;
            padding: 8px 16px;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            font-size: 0.9rem;
            color: #60a5fa;
        }

        /* Premium Stats Grid */
        .stats-grid-premium {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card-luxury {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.95) 0%, rgba(51, 65, 85, 0.95) 100%);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 25px;
            border: 1px solid rgba(71, 85, 105, 0.5);
            display: flex;
            align-items: center;
            gap: 20px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card-luxury::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, #60a5fa, #3b82f6);
        }

        .stat-card-luxury:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
            border-color: #60a5fa;
        }

        .stat-icon-luxury {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
        }

        .stat-content-luxury {
            flex: 1;
        }

        .stat-value {
            font-size: 2rem;
            font-weight: 700;
            color: #e2e8f0;
            margin-bottom: 5px;
            font-family: 'Courier New', monospace;
        }

        .stat-label {
            color: #94a3b8;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .stat-trend {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .stat-trend.positive {
            background: rgba(34, 197, 94, 0.1);
            color: #22c55e;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }

        .stat-trend.neutral {
            background: rgba(148, 163, 184, 0.1);
            color: #94a3b8;
            border: 1px solid rgba(148, 163, 184, 0.3);
        }

        /* Premium Content Grid */
        .premium-content-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 25px;
            margin-bottom: 25px;
        }

        .premium-card {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.95) 0%, rgba(51, 65, 85, 0.95) 100%);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 30px;
            border: 1px solid rgba(71, 85, 105, 0.5);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .premium-card.full-width {
            grid-column: 1 / -1;
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }

        .card-header h2 {
            font-size: 1.3rem;
            font-weight: 600;
            color: #e2e8f0;
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 0;
        }

        .card-header h2 i {
            color: #60a5fa;
        }

        .live-badge {
            background: linear-gradient(135deg, #dc2626 0%, #ef4444 100%);
            color: white;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .live-badge.pulse {
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.7; }
            100% { opacity: 1; }
        }

        .update-time {
            color: #94a3b8;
            font-size: 0.9rem;
        }

        /* Luxury Inputs */
        .luxury-input {
            width: 100%;
            padding: 15px 20px;
            background: rgba(30, 41, 59, 0.8);
            border: 1px solid #475569;
            border-radius: 12px;
            color: #e2e8f0;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .luxury-input:focus {
            outline: none;
            border-color: #60a5fa;
            box-shadow: 0 0 0 3px rgba(96, 165, 250, 0.1);
        }

        .luxury-select {
            padding: 12px 16px;
            background: rgba(30, 41, 59, 0.8);
            border: 1px solid #475569;
            border-radius: 8px;
            color: #e2e8f0;
            font-size: 0.9rem;
        }

        /* Search Widget */
        .search-input-group {
            position: relative;
            margin-bottom: 15px;
        }

        .search-input-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #94a3b8;
        }

        .search-input-group .luxury-input {
            padding-left: 45px;
        }

        .search-filters {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        /* Quick Actions */
        .quick-actions-premium {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .action-btn-premium {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.95) 0%, rgba(51, 65, 85, 0.95) 100%);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(71, 85, 105, 0.5);
            border-radius: 12px;
            padding: 20px;
            color: #e2e8f0;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 12px;
            position: relative;
        }

        .action-btn-premium:hover {
            border-color: #60a5fa;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }

        .action-icon {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.1rem;
        }

        .action-btn-premium span {
            flex: 1;
            text-align: left;
            font-weight: 500;
        }

        .action-badge {
            background: rgba(96, 165, 250, 0.1);
            color: #60a5fa;
            padding: 4px 8px;
            border-radius: 6px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        /* Market Insights */
        .market-insights {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 25px;
        }

        .insight-item {
            text-align: center;
            padding: 20px;
            background: rgba(30, 41, 59, 0.5);
            border-radius: 12px;
            border: 1px solid rgba(71, 85, 105, 0.5);
        }

        .insight-value {
            font-size: 1.8rem;
            font-weight: 700;
            color: #60a5fa;
            margin-bottom: 8px;
            font-family: 'Courier New', monospace;
        }

        .insight-label {
            color: #94a3b8;
            font-size: 0.9rem;
            margin-bottom: 10px;
        }

        .insight-change {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            padding: 4px 8px;
            border-radius: 6px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .insight-change.positive {
            background: rgba(34, 197, 94, 0.1);
            color: #22c55e;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .premium-content-grid {
                grid-template-columns: 1fr;
            }
            
            .header-content {
                flex-direction: column;
                text-align: center;
                gap: 20px;
            }
            
            .luxury-time-display {
                text-align: center;
            }
            
            .account-greeting {
                flex-direction: column;
                align-items: flex-start;
                gap: 5px;
            }
        }

        @media (max-width: 960px) {
            .luxury-dashboard {
                padding: 20px;
            }
            
            .stats-grid-premium {
                grid-template-columns: 1fr;
            }
            
            .quick-actions-premium {
                grid-template-columns: 1fr;
            }
            
            .luxury-title {
                font-size: 2rem;
            }
            
            .account-greeting {
                font-size: 1.3rem;
            }
            
            .management-card {
                padding: 20px;
            }
            
            .info-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 5px;
            }
        }

        /* =======================================
   MOBILE ONLY DASHBOARD STYLES (max-width: 960px)
   ======================================= */

@media (max-width: 960px) {
  /* Mobile container optimization */
  .luxury-dashboard {
    padding: 16px 14px 20px !important;
    margin: 0 !important;
    max-width: 100% !important;
    overflow-x: hidden !important;
  }

  /* Premium header - fitted design */
  .premium-header {
    padding: 24px 18px !important;
    margin-bottom: 20px !important;
    border-radius: 16px !important;
    background: linear-gradient(135deg, rgba(30, 41, 59, 0.98) 0%, rgba(51, 65, 85, 0.98) 100%) !important;
    border: 1px solid rgba(96, 165, 250, 0.3) !important;
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.25) !important;
    position: relative !important;
    overflow: hidden !important;
  }

  .premium-header::before {
    width: 200px !important;
    height: 200px !important;
  }

  .header-content {
    flex-direction: column !important;
    align-items: stretch !important;
    gap: 16px !important;
    text-align: center !important;
  }

  /* Account greeting - mobile optimized */
  .account-greeting {
    font-size: 1.3rem !important;
    margin-bottom: 8px !important;
    flex-direction: column !important;
    align-items: center !important;
    gap: 6px !important;
    text-align: center !important;
    word-break: break-word !important;
  }

  .role-badge {
    padding: 4px 14px !important;
    font-size: 0.9rem !important;
  }

  /* Luxury title - mobile fitted */
  .luxury-title {
    font-size: 2rem !important;
    line-height: 1.3 !important;
    margin: 0 0 8px 0 !important;
    text-align: center !important;
    word-break: break-word !important;
    hyphens: auto !important;
  }

  .luxury-subtitle {
    font-size: 1rem !important;
    margin: 0 !important;
    text-align: center !important;
    padding: 0 10px !important;
  }

  /* Time display - mobile friendly */
  .luxury-time-display {
    text-align: center !important;
    margin-top: 8px !important;
    width: 100% !important;
  }

  .time-primary {
    font-size: 1.8rem !important;
    margin-bottom: 4px !important;
  }

  .date-secondary {
    font-size: 0.95rem !important;
    margin-bottom: 12px !important;
  }

  .weather-badge {
    width: 100% !important;
    justify-content: center !important;
    padding: 10px 20px !important;
    font-size: 0.95rem !important;
  }

  /* Account management - premium mobile layout */
  .account-management-section {
    margin-bottom: 20px !important;
  }

  .management-card {
    padding: 20px 18px !important;
    border-radius: 16px !important;
    background: linear-gradient(135deg, rgba(30, 41, 59, 0.98) 0%, rgba(51, 65, 85, 0.98) 100%) !important;
    border: 1px solid rgba(96, 165, 250, 0.3) !important;
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.2) !important;
  }

  .management-header h3 {
    font-size: 1.2rem !important;
    justify-content: center !important;
    text-align: center !important;
  }

  .user-info-display {
    padding: 16px !important;
    margin-bottom: 20px !important;
  }

  .info-row {
    flex-direction: column !important;
    align-items: flex-start !important;
    gap: 4px !important;
    padding: 12px 0 !important;
  }

  .info-label {
    font-size: 0.9rem !important;
    width: 100% !important;
    text-align: left !important;
  }

  .info-value {
    font-size: 1rem !important;
    width: 100% !important;
    word-break: break-word !important;
    text-align: left !important;
  }

  .account-type {
    display: inline-block !important;
    margin-top: 4px !important;
    padding: 6px 14px !important;
    font-size: 0.9rem !important;
  }

  .update-account-link {
    margin-bottom: 20px !important;
  }

  .update-btn {
    width: 100% !important;
    justify-content: center !important;
    padding: 14px 20px !important;
    font-size: 1rem !important;
    border-radius: 12px !important;
  }

  /* Inventory management - mobile optimized */
  .inventory-management-section {
    padding: 20px !important;
    margin-top: 16px !important;
  }

  .inventory-management-section h3 {
    font-size: 1.1rem !important;
    justify-content: center !important;
    text-align: center !important;
    margin-bottom: 12px !important;
  }

  .management-description {
    text-align: center !important;
    margin-bottom: 16px !important;
    padding: 0 8px !important;
  }

  .inventory-btn {
    width: 100% !important;
    justify-content: center !important;
    padding: 14px 20px !important;
    font-size: 1rem !important;
    border-radius: 12px !important;
  }

  /* Stats grid - single column for mobile */
  .stats-grid-premium {
    grid-template-columns: 1fr !important;
    gap: 16px !important;
    margin-bottom: 20px !important;
  }

  .stat-card-luxury {
    padding: 20px !important;
    border-radius: 14px !important;
    flex-direction: column !important;
    align-items: stretch !important;
    gap: 16px !important;
    text-align: center !important;
  }

  .stat-icon-luxury {
    width: 50px !important;
    height: 50px !important;
    margin: 0 auto !important;
    border-radius: 10px !important;
    font-size: 1.3rem !important;
  }

  .stat-content-luxury {
    width: 100% !important;
  }

  .stat-value {
    font-size: 1.8rem !important;
    margin-bottom: 4px !important;
  }

  .stat-label {
    font-size: 0.9rem !important;
    margin-bottom: 8px !important;
  }

  .stat-trend {
    margin: 0 auto !important;
    padding: 8px 16px !important;
    font-size: 0.85rem !important;
  }

  /* Content grid - single column mobile layout */
  .premium-content-grid {
    grid-template-columns: 1fr !important;
    gap: 20px !important;
    margin-bottom: 20px !important;
  }

  .premium-card {
    padding: 20px 18px !important;
    border-radius: 16px !important;
    background: linear-gradient(135deg, rgba(30, 41, 59, 0.98) 0%, rgba(51, 65, 85, 0.98) 100%) !important;
    border: 1px solid rgba(96, 165, 250, 0.3) !important;
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.2) !important;
    overflow: hidden !important;
  }

  .card-header {
    flex-direction: column !important;
    align-items: flex-start !important;
    gap: 10px !important;
    margin-bottom: 20px !important;
  }

  .card-header h2 {
    font-size: 1.2rem !important;
    text-align: left !important;
    width: 100% !important;
    justify-content: flex-start !important;
  }

  .live-badge {
    align-self: flex-start !important;
    padding: 6px 14px !important;
    font-size: 0.85rem !important;
  }

  .update-time {
    align-self: flex-start !important;
    font-size: 0.85rem !important;
  }

  /* Search widget - mobile optimized */
  .search-widget-premium {
    width: 100% !important;
  }

  .search-input-group {
    margin-bottom: 12px !important;
  }

  .luxury-input {
    padding: 14px 16px 14px 45px !important;
    font-size: 1rem !important;
    border-radius: 10px !important;
  }

  .search-filters {
    flex-direction: column !important;
    gap: 12px !important;
    margin-bottom: 16px !important;
  }

  .luxury-select {
    width: 100% !important;
    padding: 12px 14px !important;
    font-size: 0.95rem !important;
    border-radius: 10px !important;
  }

  .search-results-live {
    max-height: 200px !important;
    overflow-y: auto !important;
    padding: 12px !important;
    border-radius: 10px !important;
    background: rgba(15, 23, 42, 0.5) !important;
  }

  /* Recent vehicles - mobile layout */
  .recent-vehicles {
    min-height: 120px !important;
    padding: 16px !important;
    border-radius: 10px !important;
    background: rgba(15, 23, 42, 0.5) !important;
  }

  .no-recent {
    text-align: center !important;
    padding: 20px !important;
  }

  .no-recent i {
    font-size: 2rem !important;
    margin-bottom: 12px !important;
  }

  .no-recent p {
    font-size: 1rem !important;
    margin-bottom: 6px !important;
  }

  .no-recent small {
    font-size: 0.85rem !important;
  }

  /* Inventory updates - mobile optimized */
  .inventory-updates-live {
    width: 100% !important;
  }

  .update-stream {
    margin-bottom: 16px !important;
  }

  .update-item {
    padding: 14px !important;
    margin-bottom: 10px !important;
    border-radius: 10px !important;
    background: rgba(15, 23, 42, 0.5) !important;
    border: 1px solid rgba(71, 85, 105, 0.3) !important;
  }

  .update-icon {
    width: 36px !important;
    height: 36px !important;
    border-radius: 8px !important;
    font-size: 1rem !important;
  }

  .update-content p {
    font-size: 0.95rem !important;
    margin-bottom: 4px !important;
    word-break: break-word !important;
  }

  .update-content .update-time {
    font-size: 0.8rem !important;
  }

  .inventory-stats {
    display: flex !important;
    gap: 16px !important;
    justify-content: space-between !important;
    padding: 16px !important;
    border-radius: 10px !important;
    background: rgba(15, 23, 42, 0.5) !important;
  }

  .inventory-stat {
    flex: 1 !important;
    text-align: center !important;
    padding: 12px 8px !important;
    border-radius: 8px !important;
    background: rgba(30, 41, 59, 0.8) !important;
  }

  .stat-number {
    font-size: 1.5rem !important;
    font-weight: 700 !important;
    display: block !important;
    margin-bottom: 4px !important;
    color: #60a5fa !important;
  }

  .stat-label {
    font-size: 0.85rem !important;
    color: #94a3b8 !important;
  }

  /* Quick actions - mobile optimized */
  .quick-actions-premium {
    grid-template-columns: 1fr !important;
    gap: 12px !important;
  }

  .action-btn-premium {
    padding: 16px !important;
    border-radius: 12px !important;
    gap: 14px !important;
    width: 100% !important;
    justify-content: flex-start !important;
  }

  .action-icon {
    width: 44px !important;
    height: 44px !important;
    border-radius: 10px !important;
    font-size: 1.2rem !important;
    flex-shrink: 0 !important;
  }

  .action-btn-premium span {
    font-size: 1rem !important;
    word-break: break-word !important;
    text-align: left !important;
    flex: 1 !important;
  }

  .action-badge {
    flex-shrink: 0 !important;
    padding: 5px 10px !important;
    font-size: 0.85rem !important;
  }

  /* Market insights - mobile optimized */
  .market-insights {
    grid-template-columns: 1fr !important;
    gap: 16px !important;
  }

  .insight-item {
    padding: 20px 16px !important;
    border-radius: 12px !important;
  }

  .insight-value {
    font-size: 1.6rem !important;
    margin-bottom: 6px !important;
  }

  .insight-label {
    font-size: 0.9rem !important;
    margin-bottom: 8px !important;
    word-break: break-word !important;
  }

  .insight-change {
    padding: 6px 12px !important;
    font-size: 0.85rem !important;
  }

  /* Full width card adjustments */
  .premium-card.full-width {
    margin-top: 8px !important;
  }

  /* Prevent any text/image cutting */
  * {
    box-sizing: border-box !important;
    max-width: 100% !important;
    word-wrap: break-word !important;
  }

  img, svg, canvas {
    max-width: 100% !important;
    height: auto !important;
  }

  /* Ensure proper scrolling */
  body, .luxury-theme, .luxury-dashboard {
    overflow-x: hidden !important;
    position: relative !important;
  }
}

/* =======================================
   SMALL MOBILE ENHANCEMENTS (max-width: 480px)
   ======================================= */

@media (max-width: 480px) {
  .luxury-dashboard {
    padding: 14px 12px 18px !important;
  }

  .premium-header {
    padding: 20px 16px !important;
  }

  .luxury-title {
    font-size: 1.7rem !important;
  }

  .account-greeting {
    font-size: 1.2rem !important;
  }

  .management-card,
  .premium-card {
    padding: 18px 16px !important;
  }

  .stat-card-luxury {
    padding: 18px !important;
  }

  .stat-value {
    font-size: 1.6rem !important;
  }

  .action-btn-premium {
    padding: 14px !important;
  }

  .action-icon {
    width: 40px !important;
    height: 40px !important;
    font-size: 1.1rem !important;
  }

  .insight-item {
    padding: 18px 14px !important;
  }

  .insight-value {
    font-size: 1.4rem !important;
  }
}

/* =======================================
   TABLET TRANSITION (961px and above)
   Desktop styles remain intact
   ======================================= */

@media (min-width: 961px) {
  
}
    </style>
</body>
</html>