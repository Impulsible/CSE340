<nav id="site-nav" class="site-nav" aria-label="Primary">
  <ul class="nav-list">

    <% 
      const homeActive = title && title.startsWith('CSE Motors');
      const customActive = title && title.startsWith('Custom');
      const sedanActive = title && title.startsWith('Sedan');
      const suvActive = title && title.startsWith('SUV');
      const truckActive = title && title.startsWith('Truck');
      const accountActive = title && title.startsWith('My Account');
    %>

    <!-- Home -->
    <li>
      <a href="/"
         class="<%= homeActive ? 'current-page' : '' %>"
         <%= homeActive ? 'aria-current="page"' : '' %>>
        Home
      </a>
    </li>

    <!-- Custom Shop -->
    <li>
      <a href="/custom"
         class="<%= customActive ? 'current-page' : '' %>"
         <%= customActive ? 'aria-current="page"' : '' %>>
        Custom
      </a>
    </li>

    <!-- Sedan -->
    <li>
      <a href="/sedan"
         class="<%= sedanActive ? 'current-page' : '' %>"
         <%= sedanActive ? 'aria-current="page"' : '' %>>
        Sedan
      </a>
    </li>

    <!-- SUV -->
    <li>
      <a href="/suv"
         class="<%= suvActive ? 'current-page' : '' %>"
         <%= suvActive ? 'aria-current="page"' : '' %>>
        SUV
      </a>
    </li>

    <!-- Truck -->
    <li>
      <a href="/truck"
         class="<%= truckActive ? 'current-page' : '' %>"
         <%= truckActive ? 'aria-current="page"' : '' %>>
        Truck
      </a>
    </li>

    <!-- Account -->
    <li>
      <a href="/account"
         class="<%= accountActive ? 'current-page' : '' %>"
         <%= accountActive ? 'aria-current="page"' : '' %>>
        Account
      </a>
    </li>

  </ul>
</nav>
