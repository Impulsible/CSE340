<style>
  /* =======================================
     Premium Vehicle Detail – page specific
     Matching Premium Collection styling
     ======================================= */

  .premium-page {
    padding: 14px 16px 20px;
  }

  @media (max-width: 768px) {
    .premium-page {
      padding: 12px 12px 16px;
    }
  }

  .vehicle-detail-wrapper {
    position: relative; /* needed for second border */
    margin-inline: auto;
    max-width: 1120px;
    background: #ffffff;
    border-radius: 14px;
    border: 1px solid #e3edff;
    padding: 18px 16px 20px;
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.10);
  }

  /* Second blue border (outer frame) */
  .vehicle-detail-wrapper::before {
    content: "";
    position: absolute;
    inset: -6px; /* distance for second frame */
    border-radius: 18px;
    border: 1px solid #c4ddff;
    pointer-events: none;
    z-index: -1;
  }

  @media (min-width: 960px) {
    .vehicle-detail-wrapper {
      padding: 22px 22px 24px;
      margin-top: 8px;
    }
  }

  /* On mobile, make it feel full-width like stacked premium cards */
  @media (max-width: 768px) {
    .vehicle-detail-wrapper {
      max-width: 100%;
      margin-inline: 0;
      border-radius: 10px;
    }
  }

  /* Header styling similar to premium header */
  .vehicle-detail-header {
    text-align: center;
    margin-bottom: 18px;
    padding: 18px 16px 16px;
    border-radius: 12px;
    background: linear-gradient(135deg, #eef5ff, #dbeafe);
    border: 1px solid #c4ddff;
  }

  .vehicle-detail-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 5px 12px;
    border-radius: 999px;
    background: linear-gradient(135deg, #2c63ff, #1d4ed8);
    color: #ffffff;
    font-size: 0.78rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 8px;
    box-shadow: 0 4px 12px rgba(44, 99, 255, 0.4);
  }

  .vehicle-detail-badge i {
    font-size: 0.75rem;
  }

  .vehicle-detail-title {
    font-family: Raleway, system-ui, sans-serif;
    font-weight: 800;
    font-size: clamp(1.55rem, 4.4vw, 2.05rem);
    margin: 0 0 6px;
    color: #0c1022;
  }

  .vehicle-detail-title span {
    color: #2c63ff;
  }

  .vehicle-detail-subtitle {
    margin: 0;
    font-size: 0.95rem;
    color: #3a435a;
    max-width: 34rem;
    margin-inline: auto;
    text-align: center;
  }

  /* Layout: image + info – responsive grid like premium cards layout */
  .vehicle-detail-grid {
    display: grid;
    gap: 18px;
    align-items: flex-start;
    margin-top: 18px;
  }

  @media (min-width: 880px) {
    .vehicle-detail-grid {
      grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
      gap: 20px;
    }
  }

  /* Image card – styled like vehicle-card */
  .vehicle-image-card {
    background: #ffffff;
    border-radius: 14px;
    border: 1px solid #e3edff;
    box-shadow: 0 10px 22px rgba(0, 0, 0, 0.10);
    padding: 14px 14px 16px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  @media (max-width: 480px) {
    .vehicle-image-card {
      padding: 12px 12px 14px;
    }
  }

  .vehicle-image-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 4px;
    font-size: 0.8rem;
    color: #1f2937;
    gap: 8px;
    flex-wrap: wrap;
  }

  .stock-tag,
  .image-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 5px 10px;
    border-radius: 999px;
    font-size: 0.78rem;
    font-weight: 700;
  }

  .stock-tag {
    background: #0c1022;
    color: #e5e7eb;
  }

  .image-chip {
    background: #e0f2fe;
    color: #1d4ed8;
  }

  .stock-tag i,
  .image-chip i {
    font-size: 0.7rem;
  }

  .vehicle-image-container {
    height: 260px;
    background: #eef5ff;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    border-radius: 12px;
  }

  @media (min-width: 960px) {
    .vehicle-image-container {
      height: 280px;
    }
  }

  .vehicle-main-image {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
    image-rendering: -webkit-optimize-contrast;
    image-rendering: crisp-edges;
    opacity: 0;
    transform: scale(1.02);
    transition: opacity 0.35s ease, transform 0.35s ease;
  }

  .vehicle-main-image.loaded {
    opacity: 1;
    transform: scale(1);
  }

  .vehicle-image-card:hover .vehicle-main-image {
    transform: scale(1.04);
  }

  /* Category highlight pills – like detail-item chips */
  .vehicle-category-highlights {
    margin-top: 10px;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .highlight-pill {
    flex: 1 1 150px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    padding: 6px 10px;
    border-radius: 999px;
    background: #f3f6ff;
    border: 1px solid #dbeafe;
    font-size: 0.82rem;
    font-weight: 600;
    color: #3a435a;
    text-align: center;
    min-width: 0;
    opacity: 0;
    transform: translateY(4px);
    animation: pillFadeUp 0.45s ease forwards;
    box-shadow: 0 4px 10px rgba(15, 23, 42, 0.08);
  }

  .highlight-pill i {
    font-size: 0.9rem;
    color: #2c63ff;
  }

  .highlight-pill.delay-1 {
    animation-delay: 0.08s;
  }

  .highlight-pill.delay-2 {
    animation-delay: 0.16s;
  }

  @keyframes pillFadeUp {
    from {
      opacity: 0;
      transform: translateY(4px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .vehicle-image-footer {
    margin-top: 10px;
    display: flex;
    justify-content: space-between;
    gap: 8px;
    font-size: 0.78rem;
    color: #4b5563;
    flex-wrap: wrap;
  }

  .vehicle-image-footer span {
    display: inline-flex;
    align-items: center;
    gap: 5px;
  }

  .vehicle-image-footer i {
    font-size: 0.75rem;
    color: #6b7280;
  }

  /* Info panel – styled like card body of premium grid */
  .vehicle-info-card {
    background: #ffffff;
    border-radius: 14px;
    border: 1px solid #e3edff;
    box-shadow: 0 10px 22px rgba(0, 0, 0, 0.10);
    padding: 16px 16px 18px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  @media (min-width: 960px) {
    .vehicle-info-card {
      padding: 18px 18px 20px;
    }
  }

  .price-row {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 10px;
  }

  .vehicle-price-large {
    font-size: 1.9rem;
    font-weight: 800;
    color: #059669;
    letter-spacing: 0.03em;
    white-space: nowrap;
  }

  .vehicle-price-note {
    font-size: 0.8rem;
    color: #6b7280;
    text-align: right;
    max-width: 230px;
    line-height: 1.4;
  }

  .vehicle-meta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    font-size: 0.86rem;
    color: #3a435a;
  }

  .vehicle-meta-row span {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 5px 10px;
    border-radius: 999px;
    background: #f3f6ff;
  }

  .vehicle-meta-row i {
    font-size: 0.85rem;
    color: #2c63ff;
  }

  .vehicle-details-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
  }

  @media (min-width: 600px) {
    .vehicle-details-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .detail-card {
    background: #f9fafb;
    border-radius: 10px;
    border: 1px solid #e5e7eb;
    padding: 8px 8px 10px;
    text-align: center;
    transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease;
  }

  .detail-card:hover {
    border-color: #b7d5ff;
    box-shadow: 0 10px 20px rgba(44, 99, 255, 0.18);
    transform: translateY(-1px);
  }

  .detail-label {
    font-size: 0.78rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: #6b7280;
    margin-bottom: 2px;
  }

  .detail-value {
    font-size: 0.98rem;
    font-weight: 700;
    color: #111827;
  }

  .vehicle-description {
    margin-top: 4px;
    background: #f9fafb;
    border-radius: 10px;
    border: 1px solid #e5e7eb;
    padding: 12px 12px 12px 14px;
    color: #3a435a;
    line-height: 1.6;
    font-size: 0.93rem;
    text-align: justify;
    text-justify: inter-word;
    hyphens: auto;
    position: relative;
  }

  .vehicle-description::before {
    content: "";
    position: absolute;
    left: 0;
    top: 8px;
    bottom: 8px;
    width: 3px;
    border-radius: 999px;
    background: linear-gradient(180deg, #2c63ff, #78d0ff);
  }

  .vehicle-description h3 {
    margin: 0 0 6px 6px;
    font-size: 1rem;
    color: #0c1022;
    display: flex;
    align-items: center;
    gap: 6px;
    font-family: Raleway, system-ui, sans-serif;
    font-weight: 800;
  }

  .vehicle-description h3 i {
    font-size: 0.9rem;
    color: #2c63ff;
  }

  .vehicle-description p {
    margin: 0 0 0 6px;
  }

  /* Actions – buttons match premium .view-details style */
  .vehicle-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 6px;
  }

  .vehicle-actions .btn-primary,
  .vehicle-actions .btn-secondary {
    flex: 1 1 180px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    border-radius: 10px;
    font-weight: 800;
    font-size: 0.92rem;
    text-decoration: none;
    white-space: nowrap;
    padding: 9px 14px;
    cursor: pointer;
    transition: background-color 0.2s ease, color 0.2s ease,
                box-shadow 0.2s ease, transform 0.2s ease, border-color 0.2s ease;
    text-align: center;
  }

  .vehicle-actions .btn-primary {
    background: #2c63ff;
    color: #ffffff;
    border: 2px solid #2c63ff;
    box-shadow: 0 7px 18px rgba(44, 99, 255, 0.28);
  }

  .vehicle-actions .btn-primary:hover {
    background: #1e40af;
    border-color: #1e40af;
    transform: translateY(-2px);
    box-shadow: 0 9px 22px rgba(30, 64, 175, 0.35);
  }

  .vehicle-actions .btn-secondary {
    background: #ffffff;
    color: #2c63ff;
    border: 2px solid #2c63ff;
  }

  .vehicle-actions .btn-secondary:hover {
    background: #2c63ff;
    color: #ffffff;
    transform: translateY(-2px);
    box-shadow: 0 7px 18px rgba(44, 99, 255, 0.25);
  }

  .vehicle-actions a i {
    font-size: 0.85rem;
  }

  @media (max-width: 480px) {
    .vehicle-actions .btn-primary,
    .vehicle-actions .btn-secondary {
      font-size: 0.9rem;
      padding: 8px 12px;
    }

    .price-row {
      flex-direction: column;
      align-items: flex-start;
    }

    .vehicle-price-note {
      text-align: left;
    }
  }
</style>
