<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> | CSE Motors</title>

  <!-- Global CSS (includes the long base styles) -->
  <link rel="stylesheet" href="/css/styles.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  <style>
    /* =======================================
       Premium Vehicle Detail – page specific
       Sits inside .page from styles.css
       ======================================= */

    .premium-page {
      padding: 14px 16px 20px;
    }

    @media (max-width: 768px) {
      .premium-page {
        padding: 12px 12px 16px;
      }
    }

    .vehicle-detail-wrapper {
      position: relative;
      margin-inline: auto;
      max-width: 960px;
      background: radial-gradient(circle at top left, #f5f9ff, #ffffff);
      border-radius: 18px;
      padding: 18px 14px 20px;
      margin-top: 10px;
    }

    /* outer border – same colour for true double-line look
       first line = .page border (from global CSS)
       second line = this ::before */
    .vehicle-detail-wrapper::before {
      content: "";
      position: absolute;
      inset: -10px;
      border-radius: 22px;
      border: 3px solid #78d0ff;
      pointer-events: none;
      z-index: -1;
    }

    @media (max-width: 768px) {
      .vehicle-detail-wrapper {
        max-width: 100%;
        margin-inline: 0;
        border-radius: 14px;
        padding: 16px 10px 18px;
      }

      .vehicle-detail-wrapper::before {
        inset: -6px;
        border-radius: 18px;
      }
    }

    /* Header panel – matches premium collection header card */
    .vehicle-detail-header {
      text-align: center;
      margin-bottom: 18px;
      padding: 18px 16px 16px;
      border-radius: 12px;
      background: linear-gradient(135deg, #eef5ff, #dbeafe);
      border: 1px solid #c4ddff;
    }

    .vehicle-detail-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 5px 12px;
      border-radius: 999px;
      background: linear-gradient(135deg, #2c63ff, #1d4ed8);
      color: #ffffff;
      font-size: 0.78rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
      box-shadow: 0 4px 12px rgba(44, 99, 255, 0.4);
    }

    .vehicle-detail-badge i {
      font-size: 0.75rem;
    }

    .vehicle-detail-title {
      font-family: Raleway, system-ui, sans-serif;
      font-weight: 800;
      font-size: clamp(1.55rem, 4.4vw, 2.05rem);
      margin: 0 0 6px;
      color: #0c1022;
    }

    .vehicle-detail-title span {
      color: #2c63ff;
    }

    .vehicle-detail-subtitle {
      margin: 0;
      font-size: 0.95rem;
      color: #3a435a;
      max-width: 34rem;
      margin-inline: auto;
      text-align: center;
    }

    /* Layout: image + info – responsive grid */
    .vehicle-detail-grid {
      display: grid;
      gap: 18px;
      align-items: stretch; /* make both sides same height for “app card” feel */
      margin-top: 18px;
    }

    @media (min-width: 880px) {
      .vehicle-detail-grid {
        grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
        gap: 20px;
      }
    }

    /* Image card – premium app tile style */
    .vehicle-image-card {
      background: linear-gradient(145deg, #f9fbff, #eef3ff);
      border-radius: 18px;
      border: 1px solid rgba(148, 163, 184, 0.35);
      box-shadow: 0 14px 32px rgba(15, 23, 42, 0.14);
      padding: 12px 12px 14px;
      display: flex;
      flex-direction: column;
      gap: 10px;
      height: 100%;
    }

    @media (max-width: 480px) {
      .vehicle-image-card {
        padding: 10px 10px 12px;
      }
    }

    .vehicle-image-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 6px;
      font-size: 0.78rem;
      color: #1f2937;
      gap: 8px;
      flex-wrap: wrap;
    }

    .stock-tag,
    .image-chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 5px 10px;
      border-radius: 999px;
      font-size: 0.78rem;
      font-weight: 700;
    }

    .stock-tag {
      background: #0c1022;
      color: #e5e7eb;
    }

    .image-chip {
      background: #e0f2fe;
      color: #1d4ed8;
    }

    .stock-tag i,
    .image-chip i {
      font-size: 0.7rem;
    }

    /* Compact, contained image container – like a premium app gallery */
    .vehicle-image-container {
      position: relative;
      width: 100%;
      aspect-ratio: 16 / 9;           /* keeps it compact & responsive */
      max-height: 320px;
      border-radius: 14px;
      background: radial-gradient(circle at top, #e0f2fe, #eef2ff);
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    @media (max-width: 768px) {
      .vehicle-image-container {
        aspect-ratio: 4 / 3;
        max-height: 260px;
      }
    }

    .vehicle-main-image {
      width: 100%;
      height: 100%;
      object-fit: contain;          /* full car always visible */
      display: block;
      image-rendering: -webkit-optimize-contrast;
      image-rendering: crisp-edges;
      opacity: 0;
      transform: scale(1.01);
      transform-origin: center;
      transition: opacity 0.3s ease, transform 0.3s ease;
    }

    .vehicle-main-image.loaded {
      opacity: 1;
      transform: scale(1);
    }

    .vehicle-image-card:hover .vehicle-main-image {
      transform: scale(1.03);
    }

    /* Category highlight pills */
    .vehicle-category-highlights {
      margin-top: 10px;
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .highlight-pill {
      flex: 1 1 150px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      padding: 6px 10px;
      border-radius: 999px;
      background: #f3f6ff;
      border: 1px solid #dbeafe;
      font-size: 0.82rem;
      font-weight: 600;
      color: #3a435a;
      text-align: center;
      min-width: 0;
      opacity: 0;
      transform: translateY(4px);
      animation: pillFadeUp 0.45s ease forwards;
      box-shadow: 0 4px 10px rgba(15, 23, 42, 0.08);
    }

    .highlight-pill i {
      font-size: 0.9rem;
      color: #2c63ff;
    }

    .highlight-pill.delay-1 {
      animation-delay: 0.08s;
    }

    .highlight-pill.delay-2 {
      animation-delay: 0.16s;
    }

    @keyframes pillFadeUp {
      from {
        opacity: 0;
        transform: translateY(4px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .vehicle-image-footer {
      margin-top: 8px;
      display: flex;
      justify-content: space-between;
      gap: 8px;
      font-size: 0.76rem;
      color: #4b5563;
      flex-wrap: wrap;
    }

    .vehicle-image-footer span {
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }

    .vehicle-image-footer i {
      font-size: 0.75rem;
      color: #6b7280;
    }

    /* Info panel – matching premium tile */
    .vehicle-info-card {
      background: linear-gradient(145deg, #ffffff, #f4f7ff);
      border-radius: 18px;
      border: 1px solid rgba(148, 163, 184, 0.35);
      box-shadow: 0 14px 32px rgba(15, 23, 42, 0.14);
      padding: 14px 14px 18px;
      display: flex;
      flex-direction: column;
      gap: 12px;
      height: 100%;
    }

    @media (min-width: 960px) {
      .vehicle-info-card {
        padding: 16px 16px 20px;
      }
    }

    .price-row {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 10px;
    }

    .vehicle-price-large {
      font-size: 1.9rem;
      font-weight: 800;
      color: #059669;
      letter-spacing: 0.03em;
      white-space: nowrap;
    }

    .vehicle-price-note {
      font-size: 0.8rem;
      color: #6b7280;
      text-align: right;
      max-width: 230px;
      line-height: 1.4;
    }

    .vehicle-meta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      font-size: 0.86rem;
      color: #3a435a;
    }

    .vehicle-meta-row span {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 5px 10px;
      border-radius: 999px;
      background: #f3f6ff;
    }

    .vehicle-meta-row i {
      font-size: 0.85rem;
      color: #2c63ff;
    }

    .vehicle-details-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }

    @media (min-width: 600px) {
      .vehicle-details-grid {
        grid-template-columns: repeat(4, minmax(0, 1fr));
      }
    }

    .detail-card {
      background: #f9fafb;
      border-radius: 10px;
      border: 1px solid #e5e7eb;
      padding: 8px 8px 10px;
      text-align: center;
      transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease;
    }

    .detail-card:hover {
      border-color: #b7d5ff;
      box-shadow: 0 10px 20px rgba(44, 99, 255, 0.18);
      transform: translateY(-1px);
    }

    .detail-label {
      font-size: 0.78rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: #6b7280;
      margin-bottom: 2px;
    }

    .detail-value {
      font-size: 0.98rem;
      font-weight: 700;
      color: #111827;
    }

    .vehicle-description {
      margin-top: 4px;
      background: #f9fafb;
      border-radius: 10px;
      border: 1px solid #e5e7eb;
      padding: 12px 12px 12px 14px;
      color: #3a435a;
      line-height: 1.6;
      font-size: 0.93rem;
      text-align: justify;
      text-justify: inter-word;
      hyphens: auto;
      position: relative;
    }

    .vehicle-description::before {
      content: "";
      position: absolute;
      left: 0;
      top: 8px;
      bottom: 8px;
      width: 3px;
      border-radius: 999px;
      background: linear-gradient(180deg, #2c63ff, #78d0ff);
    }

    .vehicle-description h3 {
      margin: 0 0 6px 6px;
      font-size: 1rem;
      color: #0c1022;
      display: flex;
      align-items: center;
      gap: 6px;
      font-family: Raleway, system-ui, sans-serif;
      font-weight: 800;
    }

    .vehicle-description h3 i {
      font-size: 0.9rem;
      color: #2c63ff;
    }

    .vehicle-description p {
      margin: 0 0 0 6px;
    }

    /* Actions – scoped so they don't break global .btn-primary */
    .vehicle-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 6px;
    }

    .vehicle-actions .btn-primary,
    .vehicle-actions .btn-secondary {
      flex: 1 1 180px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      border-radius: 10px;
      font-weight: 800;
      font-size: 0.92rem;
      text-decoration: none;
      white-space: nowrap;
      padding: 9px 14px;
      cursor: pointer;
      transition:
        background-color 0.2s ease,
        color 0.2s ease,
        box-shadow 0.2s ease,
        transform 0.2s ease,
        border-color 0.2s ease;
      text-align: center;
    }

    .vehicle-actions .btn-primary {
      background: #2c63ff;
      color: #ffffff;
      border: 2px solid #2c63ff;
      box-shadow: 0 7px 18px rgba(44, 99, 255, 0.28);
    }

    .vehicle-actions .btn-primary:hover {
      background: #1e40af;
      border-color: #1e40af;
      transform: translateY(-2px);
      box-shadow: 0 9px 22px rgba(30, 64, 175, 0.35);
    }

    .vehicle-actions .btn-secondary {
      background: #ffffff;
      color: #2c63ff;
      border: 2px solid #2c63ff;
    }

    .vehicle-actions .btn-secondary:hover {
      background: #2c63ff;
      color: #ffffff;
      transform: translateY(-2px);
      box-shadow: 0 7px 18px rgba(44, 99, 255, 0.25);
    }

    .vehicle-actions .btn-primary:focus-visible,
    .vehicle-actions .btn-secondary:focus-visible {
      outline: 3px solid #00c2ff;
      outline-offset: 2px;
    }

    .vehicle-actions a i {
      font-size: 0.85rem;
    }

    @media (max-width: 480px) {
      .vehicle-actions .btn-primary,
      .vehicle-actions .btn-secondary {
        font-size: 0.9rem;
        padding: 8px 12px;
      }

      .price-row {
        flex-direction: column;
        align-items: flex-start;
      }

      .vehicle-price-note {
        text-align: left;
      }
    }

    /* =======================================
   MOBILE ONLY STYLES (max-width: 768px)
   ======================================= */

@media (max-width: 768px) {
  /* Premium mobile container */
  .premium-page {
    padding: 0 !important;
    background: linear-gradient(145deg, #f5f9ff, #ffffff) !important;
  }

  /* Main wrapper - premium fitted design */
  .vehicle-detail-wrapper {
    max-width: 100% !important;
    margin: 0 !important;
    padding: 16px 14px 20px !important;
    border-radius: 0 !important;
    background: transparent !important;
    box-shadow: none !important;
    overflow: hidden !important;
  }

  .vehicle-detail-wrapper::before {
    display: none !important; /* Remove double border for cleaner mobile look */
  }

  /* Header section - premium compact design */
  .vehicle-detail-header {
    padding: 18px 16px 16px !important;
    margin: 0 0 16px !important;
    border-radius: 12px !important;
    background: linear-gradient(135deg, #eef5ff, #dbeafe) !important;
    border: 1px solid #c4ddff !important;
    box-shadow: 0 8px 24px rgba(44, 99, 255, 0.12) !important;
  }

  .vehicle-detail-badge {
    padding: 6px 16px !important;
    font-size: 0.85rem !important;
    margin-bottom: 10px !important;
  }

  .vehicle-detail-title {
    font-size: 1.8rem !important;
    line-height: 1.2 !important;
    margin: 0 0 8px !important;
    word-break: break-word !important;
    hyphens: auto !important;
  }

  .vehicle-detail-subtitle {
    font-size: 0.95rem !important;
    line-height: 1.4 !important;
    max-width: 100% !important;
    padding: 0 4px !important;
  }

  /* Grid layout - single column for mobile */
  .vehicle-detail-grid {
    display: flex !important;
    flex-direction: column !important;
    gap: 20px !important;
    margin-top: 0 !important;
  }

  /* Image card - premium fitted design */
  .vehicle-image-card {
    padding: 16px 14px 18px !important;
    border-radius: 16px !important;
    background: linear-gradient(145deg, #f9fbff, #eef3ff) !important;
    border: 1px solid rgba(148, 163, 184, 0.4) !important;
    box-shadow: 0 12px 28px rgba(15, 23, 42, 0.1) !important;
  }

  .vehicle-image-header {
    gap: 10px !important;
    margin-bottom: 12px !important;
  }

  .stock-tag,
  .image-chip {
    padding: 6px 12px !important;
    font-size: 0.85rem !important;
  }

  /* Image container - fitted, no cutting */
  .vehicle-image-container {
    aspect-ratio: 4 / 3 !important;
    max-height: 280px !important;
    min-height: 220px !important;
    border-radius: 14px !important;
    overflow: hidden !important;
    background: radial-gradient(circle at top, #e0f2fe, #eef2ff) !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
  }

  .vehicle-main-image {
    width: 100% !important;
    height: 100% !important;
    object-fit: contain !important;
    object-position: center !important;
    padding: 8px !important; /* Prevents cutting */
    max-width: 100% !important;
    max-height: 100% !important;
  }

  /* Category highlights - fitted layout */
  .vehicle-category-highlights {
    margin-top: 16px !important;
    gap: 10px !important;
  }

  .highlight-pill {
    flex: 1 1 100% !important;
    padding: 10px 14px !important;
    font-size: 0.9rem !important;
    text-align: left !important;
    justify-content: flex-start !important;
    min-width: 0 !important;
    word-break: break-word !important;
    line-height: 1.4 !important;
  }

  .vehicle-image-footer {
    margin-top: 12px !important;
    flex-direction: column !important;
    gap: 8px !important;
    font-size: 0.85rem !important;
    padding: 0 4px !important;
  }

  .vehicle-image-footer span {
    justify-content: center !important;
  }

  /* Info card - premium fitted design */
  .vehicle-info-card {
    padding: 18px 16px 20px !important;
    border-radius: 16px !important;
    background: linear-gradient(145deg, #ffffff, #f4f7ff) !important;
    border: 1px solid rgba(148, 163, 184, 0.4) !important;
    box-shadow: 0 12px 28px rgba(15, 23, 42, 0.1) !important;
  }

  .price-row {
    flex-direction: column !important;
    gap: 8px !important;
    align-items: flex-start !important;
  }

  .vehicle-price-large {
    font-size: 2rem !important;
    line-height: 1.2 !important;
  }

  .vehicle-price-note {
    text-align: left !important;
    max-width: 100% !important;
    font-size: 0.85rem !important;
    line-height: 1.4 !important;
    padding-left: 0 !important;
  }

  .vehicle-meta-row {
    gap: 10px !important;
    justify-content: center !important;
  }

  .vehicle-meta-row span {
    flex: 1 1 calc(50% - 10px) !important;
    min-width: 0 !important;
    padding: 8px 12px !important;
    font-size: 0.88rem !important;
    justify-content: center !important;
    text-align: center !important;
    word-break: break-word !important;
  }

  /* Details grid - responsive 2-column */
  .vehicle-details-grid {
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 12px !important;
  }

  .detail-card {
    padding: 12px 10px !important;
    min-height: 70px !important;
    display: flex !important;
    flex-direction: column !important;
    justify-content: center !important;
  }

  .detail-label {
    font-size: 0.82rem !important;
    margin-bottom: 4px !important;
    word-break: break-word !important;
  }

  .detail-value {
    font-size: 1rem !important;
    word-break: break-word !important;
  }

  /* Description - fitted with proper spacing */
  .vehicle-description {
    padding: 16px 16px 16px 20px !important;
    margin-top: 8px !important;
    border-radius: 12px !important;
  }

  .vehicle-description h3 {
    font-size: 1.1rem !important;
    margin: 0 0 10px 6px !important;
  }

  .vehicle-description p {
    font-size: 0.95rem !important;
    line-height: 1.5 !important;
    text-align: left !important;
    word-break: break-word !important;
    hyphens: auto !important;
    margin: 0 !important;
  }

  /* Action buttons - full width mobile friendly */
  .vehicle-actions {
    flex-direction: column !important;
    gap: 12px !important;
    margin-top: 12px !important;
  }

  .vehicle-actions .btn-primary,
  .vehicle-actions .btn-secondary {
    flex: 1 1 auto !important;
    width: 100% !important;
    padding: 14px 16px !important;
    font-size: 1rem !important;
    border-radius: 12px !important;
    text-align: center !important;
    justify-content: center !important;
  }

  /* Ensure all text is visible and properly spaced */
  * {
    box-sizing: border-box !important;
    max-width: 100% !important;
  }

  /* Remove any potential overflow issues */
  body, .page, .premium-page, .vehicle-detail-wrapper {
    overflow-x: hidden !important;
  }
}

/* =======================================
   SMALL MOBILE ENHANCEMENTS (max-width: 480px)
   ======================================= */

@media (max-width: 480px) {
  .premium-page {
    padding: 0 !important;
  }

  .vehicle-detail-wrapper {
    padding: 14px 12px 18px !important;
  }

  .vehicle-detail-header {
    padding: 16px 14px 14px !important;
  }

  .vehicle-detail-title {
    font-size: 1.6rem !important;
  }

  .vehicle-image-card,
  .vehicle-info-card {
    padding: 14px 12px 16px !important;
  }

  .vehicle-image-container {
    max-height: 240px !important;
    min-height: 200px !important;
  }

  .vehicle-details-grid {
    gap: 10px !important;
  }

  .detail-card {
    padding: 10px 8px !important;
    min-height: 65px !important;
  }

  .vehicle-meta-row span {
    flex: 1 1 100% !important;
    font-size: 0.85rem !important;
  }

  .vehicle-actions .btn-primary,
  .vehicle-actions .btn-secondary {
    padding: 12px 14px !important;
    font-size: 0.95rem !important;
  }
}

/* =======================================
   TABLET TRANSITION (769px and above)
   Keep desktop styles intact
   ======================================= */

@media (min-width: 769px) {
  .premium-page {
    padding: 14px 16px 20px !important;
  }
}
  </style>
</head>
<body>
  <a href="#vehicle-details-heading" class="skip-link">Skip to main content</a>

  <div class="page"><!-- uses global .page styles (outer border, shadow, etc.) -->
    <main id="main" class="premium-page">
      <section class="vehicle-detail-wrapper" aria-label="Vehicle details">
        <header class="vehicle-detail-header">
          <div class="vehicle-detail-badge">
            <i class="fas fa-car-side"></i>
            <%= vehicle.classification_name %>
          </div>
          <h1 id="vehicle-details-heading" class="vehicle-detail-title">
            <span><%= vehicle.inv_year %></span> <%= vehicle.inv_make %> <%= vehicle.inv_model %>
          </h1>
          <p class="vehicle-detail-subtitle">
            Premium listing · CSE Motors · Carefully inspected and ready to drive
          </p>
        </header>

        <div class="vehicle-detail-grid">
          <!-- IMAGE SIDE -->
          <article class="vehicle-image-card">
            <div class="vehicle-image-header">
              <span class="stock-tag">
                <i class="fas fa-hashtag"></i>
                Stock ID: <%= vehicle.inv_id %>
              </span>
              <span class="image-chip">
                <i class="fas fa-camera"></i>
                Actual vehicle
              </span>
            </div>

            <div class="vehicle-image-container">
              <img
                src="<%= vehicle.inv_image %>"
                alt="<%= vehicle.inv_year %> <%= vehicle.inv_make %> <%= vehicle.inv_model %>"
                class="vehicle-main-image"
                onload="this.classList.add('loaded')"
                onerror="this.src='/images/placeholder-vehicle.jpg'; this.alt='Vehicle image not available'; this.classList.add('loaded');"
              />
            </div>

            <!-- CATEGORY-AWARE PILL BADGES -->
            <div class="vehicle-category-highlights">
              <% if (vehicle.classification_name === "Sedan") { %>
                <div class="highlight-pill">
                  <i class="fas fa-car-side" aria-hidden="true"></i>
                  <span>Smooth, comfort-focused daily driver</span>
                </div>
                <div class="highlight-pill delay-1">
                  <i class="fas fa-couch" aria-hidden="true"></i>
                  <span>Spacious cabin for relaxed commuting</span>
                </div>
              <% } else if (vehicle.classification_name === "Sport") { %>
                <div class="highlight-pill">
                  <i class="fas fa-flag-checkered" aria-hidden="true"></i>
                  <span>Performance-tuned for sharp response</span>
                </div>
                <div class="highlight-pill delay-1">
                  <i class="fas fa-gauge-high" aria-hidden="true"></i>
                  <span>Engaging, driver-focused experience</span>
                </div>
              <% } else if (vehicle.classification_name === "SUV") { %>
                <div class="highlight-pill">
                  <i class="fas fa-car" aria-hidden="true"></i>
                  <span>Versatile SUV for family and cargo</span>
                </div>
                <div class="highlight-pill delay-1">
                  <i class="fas fa-mountain" aria-hidden="true"></i>
                  <span>Ready for weekend trips &amp; adventures</span>
                </div>
              <% } else if (vehicle.classification_name === "Truck") { %>
                <div class="highlight-pill">
                  <i class="fas fa-truck-pickup" aria-hidden="true"></i>
                  <span>Built for towing and hauling confidence</span>
                </div>
                <div class="highlight-pill delay-1">
                  <i class="fas fa-screwdriver-wrench" aria-hidden="true"></i>
                  <span>Durable chassis for tough conditions</span>
                </div>
              <% } else if (vehicle.classification_name === "Custom") { %>
                <div class="highlight-pill">
                  <i class="fas fa-wand-magic-sparkles" aria-hidden="true"></i>
                  <span>Unique custom build with standout style</span>
                </div>
                <div class="highlight-pill delay-1">
                  <i class="fas fa-screwdriver-wrench" aria-hidden="true"></i>
                  <span>Upgrades beyond standard factory spec</span>
                </div>
              <% } else { %>
                <!-- Classic / Other -->
                <div class="highlight-pill">
                  <i class="fas fa-medal" aria-hidden="true"></i>
                  <span>Classic collector vehicle with iconic styling</span>
                </div>
                <div class="highlight-pill delay-1">
                  <i class="fas fa-star" aria-hidden="true"></i>
                  <span>Ideal for shows, cruises &amp; enthusiasts</span>
                </div>
              <% } %>
            </div>

            <div class="vehicle-image-footer">
              <span>
                <i class="fas fa-location-dot"></i>
                View at CSE Motors showroom
              </span>
              <span>
                <i class="fas fa-clock"></i>
                Book a test drive today
              </span>
            </div>
          </article>

          <!-- INFO SIDE -->
          <article class="vehicle-info-card">
            <div class="price-row">
              <div class="vehicle-price-large">
                $<%= vehicle.inv_price.toLocaleString() %>
              </div>
              <div class="vehicle-price-note">
                <i class="fas fa-info-circle"></i>
                Price excludes tax, registration &amp; optional warranty.
              </div>
            </div>

            <div class="vehicle-meta-row">
              <span>
                <i class="fas fa-gauge-high"></i>
                <%= vehicle.inv_miles.toLocaleString() %> miles
              </span>
              <span>
                <i class="fas fa-palette"></i>
                Exterior: <%= vehicle.inv_color %>
              </span>
              <span>
                <i class="fas fa-tags"></i>
                Category: <%= vehicle.classification_name %>
              </span>
            </div>

            <div class="vehicle-details-grid">
              <div class="detail-card">
                <div class="detail-label">Year</div>
                <div class="detail-value"><%= vehicle.inv_year %></div>
              </div>
              <div class="detail-card">
                <div class="detail-label">Make</div>
                <div class="detail-value"><%= vehicle.inv_make %></div>
              </div>
              <div class="detail-card">
                <div class="detail-label">Model</div>
                <div class="detail-value"><%= vehicle.inv_model %></div>
              </div>
              <div class="detail-card">
                <div class="detail-label">Mileage</div>
                <div class="detail-value"><%= vehicle.inv_miles.toLocaleString() %></div>
              </div>
            </div>

            <div class="vehicle-description">
              <h3>
                <i class="fas fa-clipboard-list"></i>
                Vehicle Description
              </h3>
              <p><%= vehicle.inv_description %></p>
            </div>

            <div class="vehicle-actions">
              <a href="/" class="btn-secondary">
                <i class="fas fa-home"></i>
                Back to Home
              </a>
              <a href="/contact?vehicle=<%= vehicle.inv_id %>" class="btn-primary">
                <i class="fas fa-envelope"></i>
                Contact about this vehicle
              </a>
              <a href="/inv/type/<%= vehicle.classification_id %>" class="btn-secondary">
                <i class="fas fa-arrow-left"></i>
                View similar vehicles
              </a>
            </div>
          </article>
        </div>
      </section>
    </main>
  </div>
</body>
</html>